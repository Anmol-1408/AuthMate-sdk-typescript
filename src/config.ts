export default {
    AUTHMATE_BASE_API_URL : "https://backend.authmate.xyz" // Replace with your actual API URL

} as const // This is a TypeScript module that exports a constant object with the API_URL property.
// This is a TypeScript module that exports a constant object with the API_URL property.

// export const API_URL = process.env.API_URL || "http://localhost:3000/api"; // Default to localhost if not set